# Use read-only Gmail access

The tracker requests Gmail read-only authorization only. It needs message access for synchronization and on-demand Inbox review, but it must never send, modify, label, or delete Gmail messages.
